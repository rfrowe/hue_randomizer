# Hue Bridge Configuration
# You can find this at https://discovery.meethue.com
HUE_BRIDGE_HOST=your-bridge-hostname.local
HUE_API_KEY=your-api-key-here

# Effect Configuration (in seconds)
EFFECT_DURATION=10

# Transition time (0 = instant, higher = slower fade)
TRANSITION_TIME=0
